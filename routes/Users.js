const express = require('express');
const router  = express.Router()

// /
// router.route('/').get(getAllStudents).post(createStudent)

// /userId
// router.route('/:userId').get(getOneStudent).put(updateStudent)

// Create one
router.post('/', (req, res) => {
    
});

// Update one

// Delete one
module.exports = router